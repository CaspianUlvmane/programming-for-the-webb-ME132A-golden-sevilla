function like (liked){
    create _likeHTML
    if (liked){
        _likeHTML dark heart
    } else {
        _likeHTML light heart
    }
    addEventListener (click, PopUp(liked))
    return _likeHTML
}

function PopUp (liked){
    if (parentEmpty) =
        create popUpDiv
        append popUpDiv to parentDiv
        loop through liked
            likedItem
            addEventListener (click, removeLike(liked))
            append likedItem to popUpDiv
    else
        empty parentDiv
}

function removeLike (liked){
    preventDefault
    find this likedItem
    remove likedItem from liked
}

function renderPopUp