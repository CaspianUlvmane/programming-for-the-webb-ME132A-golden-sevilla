function buildTopMenu (searching, liked)
    createDiv menuHTML
    append burgerHTML() to menuHTML
    append logoHTML to menuHTML
    if (searching)
        append isSearchingHTML to menuHTML
    else
        append searchHTML to menuHTML
    
    append likeHTML to menuHTML
    return menuHTML

function burgerHTML ()
    array burgerArray
        buttonText_uLivet, buttonURL_uLivet
        buttontext_continent, buttonURL_continent

    createDiv _burgerHTML
    loop through burgerArray
        createDiv burgerItem
        fix burgerItem with element information
        append burgerItem to _burgerHTML
        
    return _burgerHTML